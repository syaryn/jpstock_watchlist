[tools]
prek = "latest"
python = "latest"
ruff = "latest"
ty = "latest"
uv = "latest"

[tasks]
"fmt" = "ruff format ."
"lint" = "ruff check --fix ."
"type" = "ty check"
"test" = "pytest"
"all" = { depends = ["fmt", "lint", "type", "test"] }
"analyze" = "uv run python main.py"
